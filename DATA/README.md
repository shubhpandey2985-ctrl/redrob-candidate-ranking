# Data Folder

Copy the organizer-provided full dataset here before running:

```text
data/candidates.jsonl
```

The repository includes `sample_candidates.json` and `sample_submission.csv` for schema inspection only.
